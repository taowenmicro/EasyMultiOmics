% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_resall_with_tree.R
\name{plot_circular_diffheatmap}
\alias{plot_circular_diffheatmap}
\title{Circular Heatmap for Differential Microbial Analysis}
\usage{
plot_circular_diffheatmap(
  details_tab,
  summary_tab,
  topN = 30,
  type = c("full", "half", "3/4"),
  inner_gap = 0,
  label_size = 3,
  tip_order = NULL
)
}
\arguments{
\item{details_tab}{data.frame A detailed results table with columns:
\describe{
  \item{micro}{Microbe/OTU/ASV name}
  \item{method}{Name of the differential analysis method}
  \item{sig}{Significance marker, can be "显著"/"不显著" or 1/0}
}}

\item{summary_tab}{data.frame A summary results table with columns:
\describe{
  \item{micro}{Microbe/OTU/ASV name (must match details_tab)}
  \item{n_methods}{Number of methods that detected this microbe as significant}
}}

\item{topN}{integer Number of top OTUs/ASVs to display (ranked by n_methods).}

\item{type}{character Layout type, one of `"full"`, `"half"`, or `"3/4"`.}

\item{inner_gap}{numeric Inner space adjustment (controls the inner radius of the ring).}

\item{label_size}{numeric Font size for the outer labels.}

\item{tip_order}{character Optional vector specifying the OTU/ASV order on the circle.
Typically extracted from a phylogenetic tree tip order. If `NULL`, microbes are
ordered by `n_methods` in summary_tab.}
}
\value{
A ggplot2 object representing the circular heatmap.
}
\description{
Generate a circular heatmap that shows the consistency of differential analysis
results across multiple methods. Supports full circle, half circle, and 3/4 circle layouts.
}
\examples{
\dontrun{
p <- plot_circular_diffheatmap(details_tab, summary_tab, topN = 30, type = "full")
print(p)
}
}
