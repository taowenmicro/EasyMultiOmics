% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wgcna.hub.trans.R
\name{wgcna.hub.trans}
\alias{wgcna.hub.trans}
\title{Perform WGCNA analysis on transcriptome functional composition table}
\usage{
wgcna.hub.trans(
  ps = ps.trans,
  data_type = "TPM",
  group = "Group",
  WGCNApath = NULL,
  alltraits = NULL,
  sample_filter = FALSE,
  clustsample_thresold = 50000,
  minModuleSize = 30,
  MEDissThres = 0.25,
  topnSelect_module = 5,
  nSelect_genes = 400
)
}
\arguments{
\item{ps}{A phyloseq object containing transcriptome functional composition data and sample metadata.}

\item{data_type}{Transcriptome data type. Must be either "counts" or "TPM"/"FPKM".
If "counts", will apply log2(counts+1) transformation. Default is "TPM".}

\item{group}{Character specifying the grouping variable in sample metadata. Default is "Group".}

\item{WGCNApath}{Character specifying output directory path. If NULL (default), no files will be saved.}

\item{alltraits}{Optional data frame of additional traits (samples in rows, traits in columns).
Default NULL uses the grouping variable as the sole trait.}

\item{sample_filter}{Logical indicating whether to filter outlier samples. Default TRUE.}

\item{minModuleSize}{Integer specifying minimum module size. Default 30.}

\item{MEDissThres}{Numeric threshold (0-1) for merging similar modules. Default 0.75.}

\item{topnSelect_module}{Integer specifying number of top modules to select. Default 5.}

\item{nSelect_genes}{Integer specifying number of top genes to display. Default 400.}

\item{clustsample_threshold}{Numeric threshold for sample clustering (only if sample_filter=TRUE). Default 50000.}
}
\description{
This function conducts Weighted Gene Co-expression Network Analysis (WGCNA) including data preprocessing,
module identification, trait correlation analysis, and network visualization.
}
\examples{
\dontrun{
library(WGCNA)
library(dplyr)
data(ps.trans)
res <- wgcna.trans(ps=ps.trans, data_type="TPM",group="Group",WGCNApath=NULL,
                   alltraits=NULL,sample_filter=TRUE,clustsample_thresold=50000,
                   minModuleSize = 30,MEDissThres = 0.25,topnSelect_module=5,
                   nSelect_genes = 400)
head(res[[1]])
head(res[[2]])
}
}
\author{
Tao Wen \email{2018203048@njau.edu.cn},
Peng-Hao Xie \email{2019103106@njau.edu.cn}
}
