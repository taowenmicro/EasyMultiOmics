% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_arc_between.R
\name{geom_arc_between}
\alias{geom_arc_between}
\title{Draw a curved line (circular arc) between two points}
\usage{
geom_arc_between(
  mapping = NULL,
  data = NULL,
  ...,
  curv = 1,
  direction = 1,
  n = 100,
  lineend = "round",
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping, data, ...}{Standard ggplot2 mapping/data/params passed to `geom_path`.
Aesthetics required: `x`, `y`, `xend`, `yend`.}

\item{curv}{Numeric in [0, 1]. 0 = straight; 1 = semicircle. Default 1.}

\item{direction}{Integer, either `1` or `-1`, chooses the side of the arc. Default 1.}

\item{n}{Integer, number of points used to draw the arc. Default 100.}

\item{lineend}{Passed to `geom_path`. Default "round".}

\item{na.rm}{Logical; default FALSE.}

\item{show.legend}{Logical; default NA.}

\item{inherit.aes}{Logical; default TRUE.}
}
\value{
A ggplot2 layer (path).
}
\description{
`geom_arc_between()` draws a circular arc between `(x, y)` and `(xend, yend)`.
The arc curvature is controlled by `curv` in [0, 1], where `1` is a semicircle,
`0` is a straight line. Use `direction = 1` or `-1` to flip the side of the arc.
}
\examples{
library(ggplot2)
df <- data.frame(x = 0, y = 0, xend = 4, yend = 0)

# semicircle above
ggplot(df) +
  geom_point(aes(x, y)) +
  geom_point(aes(xend, yend), color = "red") +
  geom_arc_between(aes(x = x, y = y, xend = xend, yend = yend),
                   curv = 1, direction = 1, linewidth = 1) +
  coord_equal() + theme_minimal()

# gentler arc below
ggplot(df) +
  geom_arc_between(aes(x, y, xend = xend, yend = yend),
                   curv = 0.4, direction = -1, linewidth = 1) +
  coord_equal() + theme_minimal()

# multiple pairs
df2 <- data.frame(
  x = c(0, 1), y = c(0, 0),
  xend = c(4, 3), yend = c(0, 1),
  curv = c(1, 0.6), direction = c(1, -1)
)
ggplot(df2) +
  geom_arc_between(aes(x, y, xend = xend, yend = yend,
                       curv = curv, direction = direction),
                   linewidth = 1) +
  coord_equal() + theme_minimal()

}
