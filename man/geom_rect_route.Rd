% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_rect_route.R
\name{geom_rect_route}
\alias{geom_rect_route}
\title{Rectangular route with two filleted right-angle bends}
\usage{
geom_rect_route(
  mapping = NULL,
  data = NULL,
  ...,
  axis = c("x", "y"),
  rx1 = 0.3,
  ry1 = 0.3,
  rx2 = 0.3,
  ry2 = 0.3,
  n = 30,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping, data, ...}{ggplot2 args. Required: x,y,xend,yend.}

\item{axis}{"x" or "y". If "x", path goes A -> (xB,yA) -> (xB,yB) = B.
If "y", path goes A -> (xA,yB) -> (xB,yB) = B.}

\item{rx1, ry1}{Radius at first corner (circular if equal).}

\item{rx2, ry2}{Radius at second corner.}

\item{n}{Number of points per arc.}
}
\description{
Connect (x,y) to (xend,yend) via two orthogonal bends so that
A, P1, P2, B form the corners of a rectangle. At both bends,
replace the sharp 90° angle with a quarter-circle/ellipse.
}
\examples{
library(ggplot2)
df <- data.frame(x=0,y=0,xend=5,yend=3)

ggplot(df) +
  geom_rect_route(aes(x,y,xend=xend,yend=yend),
                  axis="x", rx1=0.5, ry1=0.5,
                  rx2=0.3, ry2=0.3, linewidth=1) +
  coord_equal() + theme_minimal()

ggplot(df) +
  geom_rect_route(aes(x,y,xend=xend,yend=yend),
                  axis="y", rx1=0.4, ry1=0.6,
                  rx2=0.6, ry2=0.4, linewidth=1) +
  coord_equal() + theme_minimal()

}
