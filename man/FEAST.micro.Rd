% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FEAST.micro.R
\name{FEAST.micro}
\alias{FEAST.micro}
\title{FEAST Microbial traceability analysis (package API)}
\usage{
FEAST.micro(
  ps = NULL,
  otu = NULL,
  map = NULL,
  group = "Group",
  sinkG = NULL,
  sourceG = NULL,
  rep = NULL,
  em_iter = 1000
)
}
\arguments{
\item{ps}{A \code{phyloseq} object (可选).}

\item{otu}{OTU/ASV abundance table (taxa as rows, samples as columns).}

\item{map}{Sample metadata (rownames must be sample IDs).}

\item{group}{Column name in metadata used as group label.}

\item{sinkG}{The sink group label (single string).}

\item{sourceG}{The source group label(s), character vector.}

\item{rep}{Integer; biological replicates per group (auto-detected if NULL).}

\item{em_iter}{Integer; EM iterations for FEAST (default 1000).}
}
\value{
A data.frame: rows = sources(+unknown), cols = sink samples (replicates).
}
\description{
面向 R 包的 FEAST 包装函数。支持两种输入：
1) 直接传 phyloseq 对象 `ps`（推荐）；或
2) 传 `otu` / `map`（行=OTU/ASV，列=样本；map 行名=样本）。

函数内部会调用 FEAST 引擎（\code{FEAST_core}）对每个 sink 重复进行源追踪估计，
并返回 source × sink 的比例表。
}
\examples{
\dontrun{
  res <-  FEAST.micro(ps = ps, group = "Group",
                     sinkG = "WT", sourceG = c("KO","OE"))
}
}
