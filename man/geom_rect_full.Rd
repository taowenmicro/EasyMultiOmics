% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_rect_full.R
\name{geom_rect_full}
\alias{geom_rect_full}
\title{Rectangle path with two rounded corners (A-P1-P2-B)}
\usage{
geom_rect_full(
  mapping = NULL,
  data = NULL,
  ...,
  height = NULL,
  width = NULL,
  rx1 = 0.3,
  ry1 = 0.3,
  rx2 = 0.3,
  ry2 = 0.3,
  n = 30,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping, data, ...}{ggplot2 arguments. Required: x,y,xend,yend.}

\item{height, width}{Numeric. Offsets for the rectangle side not defined by A,B.
If A,B share y (horizontal edge), use `height` (rectangle height).
If A,B share x (vertical edge), use `width` (rectangle width).}

\item{rx1, ry1}{Fillet radii at P1.}

\item{rx2, ry2}{Fillet radii at P2.}

\item{n}{Points per fillet arc.}
}
\description{
Draw a rectangular polyline from A(x,y) to B(xend,yend),
going through the two opposite vertices P1 and P2 so that
A, P1, P2, B form a rectangle. At P1 and P2, replace sharp
right angles with circular/elliptical fillets.
}
\examples{
library(ggplot2)
df <- data.frame(x=0,y=0,xend=4,yend=0)

ggplot(df) +
  geom_rect_full(aes(x,y,xend=xend,yend=yend),
                 height=3,
                 rx1=0.4,ry1=0.4,
                 rx2=0.4,ry2=0.4,
                 linewidth=1) +
  coord_equal() + theme_minimal()

}
